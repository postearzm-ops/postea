# Procfile para Railway
# Define cómo Railway debe ejecutar tu aplicación

# Backend con migraciones automáticas
web: cd packages/backend && npm run start:migrate

# Worker para cron jobs (opcional, si se separa del web)
# worker: cd packages/backend && npm run worker
